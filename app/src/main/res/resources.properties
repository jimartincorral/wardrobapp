# Which language the unqualified `values/` directory is written in.
#
# Required by androidResources.generateLocaleConfig in build.gradle.kts: the
# generated locale list has to name a default, and nothing about a directory
# called `values` says which language is inside it. The build fails without this
# rather than guessing.
unqualifiedResLocale=en-US
